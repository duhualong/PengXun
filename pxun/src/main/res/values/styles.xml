<resources>
    <!-- 默认主题 -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light">
        <item name="windowActionBar">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="colorPrimary">@color/chatColor</item>
        <item name="colorPrimaryDark">@color/chatColor</item>
        <item name="colorAccent">@color/chatColor</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>


    <!-- SplashActivity主题 -->
    <style name="SplashTheme">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- BigPicActivity主题 -->
    <style name="BigPicTheme" parent="AppTheme">
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowBackground" >@color/translucent</item>
    </style>

    <!-- 分隔线样式 -->
    <style name="DivisionLine">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0.1dp</item>
        <item name="android:background">#888</item>
    </style>

</resources>
